#include "main.h"

/**
 * _islower - checks for lower case characters
 * @c: to check if it is lowercase
 * Return: 0 if c is lowercase, 1 if otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
