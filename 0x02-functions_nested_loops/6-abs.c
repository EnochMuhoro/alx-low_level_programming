#include "main.h"

/**
 * _abs - compute the absolute value of an integer
 * @i:the int to check
 * Return: absolute value for the int
 */

int _abs(int i)
{
	if (i >= 1)
	{
		return (i);
	}
	return (-i);
}
