#include "main.h"

/**
 * mul - multiplies two integers
 * @a: integer to multiply to b
 * @b: integer to multiply to a
 * Return: return the multiple of a and b
 */
int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
