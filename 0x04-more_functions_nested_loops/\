#include <stdio.h>
#include "main.h"

/**
 * main - print numbers from 0 to 100, followed by a new line
 * but for multiples of 3 print fizz
 * for multiples of 5 print buzz
 * numbers which are multiple of both 3 and 5 print fizzbuzz
 */
int main(void)
{
	int x;

	for (x = 0; x <= 100; x++)
	{
		if (x % 3 == 0 && x % 5 != 0)
		{
			printf("fizz");
		}
		else if (x % 5 == 0 && X % 3 != 0)
		{
			printf("buzz");
		}
		else if (x % 3 == && x % 5 == 0)
		{
			printf("fizzbuzz");
		}
		else if (x == 1)
		{
			printf("%d", x);
		}
		else
		{
			printf(" %d", x)
		}
	}
	printf("\n");

	return (0);
}
