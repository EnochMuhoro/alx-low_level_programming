#include "main.h"

/**
 * _isupper - checks for uppercase characters
 * @c: the character to look
 * Return: return the characters that are uppercase
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z')
}
